<!DOCTYPE html>

<title>New AnimalEntity</title>

{% block body %}
    <h1>Create new AnimalEntity</h1>

    {{ include('animal/_form.html.twig') }}

    <a href="{{ path('app_animal_index') }}">back to list</a>
{% endblock %}
