<!DOCTYPE html>

<title>Edit AnimalEntity</title>

{% block body %}
    <h1>Edit AnimalEntity</h1>

    {{ include('animal/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_animal_index') }}">back to list</a>

    {{ include('animal/_delete_form.html.twig') }}
{% endblock %}
